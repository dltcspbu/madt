language: python
addons:
  apt:
    packages:
      - "python3-pip"
python:
  - "3.6"
before_install:
  - chown -R $USER:$USER /usr/lib/python3/dist_packages
install:
  - make && make install
script:
  - python3 -m unittest discover -s unit_tests -p "test_*.py"
